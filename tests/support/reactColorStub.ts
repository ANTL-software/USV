import { createElement } from 'react';
import type { ReactElement } from 'react';

export function SketchPicker(): ReactElement {
  return createElement('div', { 'data-testid': 'react-color-stub' });
}
